<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items, :theme => "twitter-bootstrap-3",
    :pagination_class => "pagination-sm" %>
<% end %>
